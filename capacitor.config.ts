import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projet4-test-git',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
